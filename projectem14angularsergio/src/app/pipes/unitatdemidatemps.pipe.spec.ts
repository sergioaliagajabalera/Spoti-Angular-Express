import { UnitatdemidatempsPipe } from './unitatdemidatemps.pipe';

describe('UnitatdemidatempsPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitatdemidatempsPipe();
    expect(pipe).toBeTruthy();
  });
});
