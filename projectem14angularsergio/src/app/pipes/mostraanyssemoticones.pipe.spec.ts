import { MostraanyssemoticonesPipe } from './mostraanyssemoticones.pipe';

describe('MostraanyssemoticonesPipe', () => {
  it('create an instance', () => {
    const pipe = new MostraanyssemoticonesPipe();
    expect(pipe).toBeTruthy();
  });
});
